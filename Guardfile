guard 'livereload' do
  # We watch the client.
  watch(%r{Merchant/frontend/.+\.(css|js|ejs|html)})

  # We watch the server. Path to customize for your case.
  watch(%r{Merchant/backend/start.log})
end